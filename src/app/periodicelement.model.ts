/*

export interface PeriodicElement {
    id: number;  
    nome: string;
    email: string;
    editar: string;
  }
*/

  export interface PeriodicElement {
    position: number;  
    name: string;
    weight: number;
    symbol: string;
  }

 